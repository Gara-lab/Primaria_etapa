# Python deps for tools/*.py. Install only what your current task needs into this
# project's .venv (see CLAUDE.md's Environment section) — this file documents what
# each package is for, it is not meant to be pip-installed in full by default.
#     .venv\Scripts\pip.exe install -r tools\requirements.txt      # Windows, all of it
# Also required on PATH (not pip): ffmpeg + ffprobe, and Node.js for the .mjs tools
# and the Remotion render step.

requests                  # transcribe.py, gen_clip.py, gen_video.py, notion_sync.py, gen_music.py, gen_sfx.py
Pillow                    # gen_thumbnail.py, cutout.py, composite_logo.py, thumb_scrim.py
google-genai              # gen_image.py, gen_thumbnail.py (Gemini / IMAGE_PROVIDER default)

# YouTube stats + upload (tools/yt_stats.py, tools/yt_upload.py) — OAuth via .youtube/ token files:
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# Optional — tools/cutout.py's preferred matting method (falls back to a pure-Python key otherwise):
#   pip install rembg
# Optional — tools/cutout.py's numpy-accelerated fallback matte (~100x faster than pure Python):
#   pip install numpy
# Optional — tools/capture_web.py (auto-capturing web pages/screencasts):
#   pip install playwright && playwright install chromium
